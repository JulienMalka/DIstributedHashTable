* [-2 malus] There are various occurrences of things like

      for (int i = 0; i < some_size; ...)`

  Remember that if `some_size` is a `size_t` (which is usually should be) and is
  really large, this may loop forever (due to the size constraints on `int`s).
  Check the compiler warnings!
